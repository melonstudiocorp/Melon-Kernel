#pragma once

// #########################################
// ========== GAMES / DEMOS ================

/**
 * @brief Starfall animation (ASCII).
 * 
 * Displays a falling stars effect in the terminal.
 * Intended for demo or game screens.
 */
void starfall();

/**
 * @brief Spinner animation (ASCII).
 * 
 * Displays a spinning cursor animation in the terminal.
 * Can be used as a loading indicator.
 */
void spinner();